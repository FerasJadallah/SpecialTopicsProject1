@model List<string>

@{
    ViewBag.Title = "Files";
}

<h2>Files</h2>

<ul>
    @foreach (var file in Model)
    {
        <li>
            <a href="@Url.Action("Download", new { fileName = file })">@file</a>
        </li>
    }
</ul>
